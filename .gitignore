# --- 1. FILE SENSIBILI (FONDAMENTALE) ---
data_collector/credentials.json
credentials.json
nginx-selfsigned.crt
nginx-selfsigned.key
certs/
.env

# --- 2. AMBIENTE VIRTUALE ---
.venv/
venv/
env/

# --- 3. FILE TEMPORANEI PYTHON ---
__pycache__/
*.py[cod]
*$py.class

# --- 4. FILE DI SISTEMA E IDE ---
.DS_Store
.vscode/

# --- 5. DATI E LOG ---
postgres_data/
pgdata/
*.log

# --- 6. KUBERNETES SECRETS (Corretto) ---
# Ignora qualsiasi file yaml che contenga la parola "secret" dentro la cartella k8s
k8s/*secret*.yaml
k8s/*secret*.yml

# --- 7. FILE LEGACY (DOCKER COMPOSE) ---
# Rimosso il punto iniziale per farlo funzionare davvero
docker-compose.yaml
docker-compose.yml